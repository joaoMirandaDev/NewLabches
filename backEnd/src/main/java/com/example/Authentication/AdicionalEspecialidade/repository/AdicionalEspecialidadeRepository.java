package com.example.Authentication.AdicionalEspecialidade.repository;

import com.example.Authentication.AdicionalEspecialidade.model.AdicionalEspecialidade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdicionalEspecialidadeRepository extends JpaRepository<AdicionalEspecialidade, Integer> {
}
